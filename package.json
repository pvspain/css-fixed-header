{
  "name": "fixed-header",
  "version": "0.0.1",
  "description": "css -  howto create a fixed header, with steps captured as git versions",
  "main": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [ "css", "snippet","howto", "fixed", "header" ],
  "author": "pvspain",
  "license": "Apache-2.0",
  "devDependencies": {
    "gulp": "^3.9.0",
    "gulp-connect": "^2.2.0"
  }
}
